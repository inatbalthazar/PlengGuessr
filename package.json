{
  "name": "plengguessr",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build",
    "deploy": "echo 'Deployment ready'"
  }
}
